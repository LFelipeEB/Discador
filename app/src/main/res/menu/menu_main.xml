<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/action_search" android:icon="@drawable/search"
        android:title="@string/action_search" android:showAsAction="always"
        android:actionViewClass="android.widget.SearchView"
        tools:ignore="AppCompatResource" />
    <item android:id="@+id/action_search2" android:icon="@drawable/search"
        android:title="@string/action_search" android:showAsAction="always"
        tools:ignore="AppCompatResource" />

    <item android:id="@+id/sobre" android:icon="@drawable/information"
        android:title="@string/sobre" android:showAsAction="never"
        tools:ignore="AppCompatResource" />
</menu>
